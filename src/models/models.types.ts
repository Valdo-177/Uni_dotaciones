export interface TypesBeneficios {
    img: string;
    label: string;
    description: string;
}

export interface categoriasType {
    value: string;
    label: string;
}

export interface categoryCardType {
    img: string;
    title: string;
}